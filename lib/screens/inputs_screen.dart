import 'package:flutter/material.dart';

class InputsSreen extends StatelessWidget {
  const InputsSreen ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Inputs'),),
    );
  }
}